{
  "buildCommand": "cd ../.. && pnpm install && npx prisma generate --schema=packages/db/prisma/schema.prisma && pnpm turbo build --filter=@mock-prep/web",
  "installCommand": "echo 'handled in buildCommand'"
}
