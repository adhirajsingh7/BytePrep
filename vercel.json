{
  "buildCommand": "pnpm --filter @mock-prep/db exec prisma generate && pnpm turbo build --filter=@mock-prep/web",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/web/.output",
  "framework": null
}
